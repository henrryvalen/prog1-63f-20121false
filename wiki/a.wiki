#summary One-sentence summary of this page.

= Introduction =

Add your content here.


= Details =

Add your content here.  Format your content with:
  * Text in *bold* or _italic_
  * Headings, paragraphs, and lists
  * Automatic links to other wiki pages
<body>
	<% String apellidos = (String)request.getAttribute("apellidos"); 
	String nombres = (String)request.getAttribute("nombres");
	String genero = (String)request.getAttribute("genero");
	String fechaNacimiento = (String)request.getAttribute("fechaNacimiento");
	String pais = (String)request.getAttribute("pais");
	String[] intereses = (String[])request.getAttribute("intereses");
	String comentario = (String)request.getAttribute("comentario");
	%>
	Bienvenido a los JSP <%=apellidos%>, <%=nombres%> tu genero es <%=genero%><br/>
	tu fecha de nacimiento es <%=fechaNacimiento%> y provienes de <%=pais%><br/>
	tus intereses son <br/>
	<% if(intereses!=null){
			for(int i=0;i<intereses.length;i++){ %>
			<%=intereses[i]%> <br/>
	<%   } 
	     }%>		
	<br/>
	Escribiste este comentario <%=comentario%>
	     
	
</body>
</html>